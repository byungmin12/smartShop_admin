import { configureStore } from '@reduxjs/toolkit';import {dataLength} from "../slice/dataLength";import {randomRefresh} from "../slice/randomRefresh";import {currentPage} from "../slice/currentPage";const store = configureStore({    reducer: {        dataLength: dataLength.reducer,        randomRefresh: randomRefresh.reducer,        currentPage : currentPage.reducer    },});export type RootState = ReturnType<typeof store.getState>export type AppDispatch = typeof store.dispatchexport default store