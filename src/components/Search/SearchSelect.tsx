import React from 'react';interface ISearchSelect {    value: string | number;    options : string[] | number[];    handlerChangeCategory: (e: React.ChangeEvent<HTMLSelectElement>)=>void}function SearchSelect({value,options,handlerChangeCategory}:ISearchSelect) {    return (        <select value={value} onChange={handlerChangeCategory} name="search filter">            {                options.map((option)=> <option value={option} key={option}>{option}</option> )            }        </select>    );}export default SearchSelect