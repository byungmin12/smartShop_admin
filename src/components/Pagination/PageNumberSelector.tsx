import React from 'react';interface IPageNumberSelector {    pageLimit:number    handlerChangePageLimit: (e: React.ChangeEvent<HTMLSelectElement>)=>void}function PageNumberSelector({pageLimit,handlerChangePageLimit}:IPageNumberSelector) {    const optionValue = [10,20,50]    return (        <div>            <span>페이지 당 행: </span>            <select onChange={handlerChangePageLimit} value={pageLimit}>                {                    optionValue.map((option)=><option value={option} key={option}>{option}</option> )                }            </select>        </div>    );}export default PageNumberSelector;