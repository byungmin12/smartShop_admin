import React from 'react';export interface IPageNavigation {    handlerPrevNext: (direction : "prev" | "next") => ()=>void;    buttons: Array<string|number>    currentPage:number;    handlerSetCurrentPage: (page:number)=> () =>void }function PageNavigation({handlerPrevNext,buttons,handlerSetCurrentPage,currentPage}:IPageNavigation) {    return (        <div className="pagiNation-Wrapper">            <button onClick={handlerPrevNext("prev")} type="button">{"<"}</button>            {                buttons.map((button,idx)=>{                    if(typeof button ==="string"){                        return <button type="button" key={idx+Math.random()}>...</button>                    }                        return <button                            className={currentPage===button ? "selected" : ""}                            onClick={handlerSetCurrentPage(button)} type="button" key={button}>{button}</button>                })            }            <button onClick={handlerPrevNext("next")} type="button">{">"}</button>        </div>    );}export default React.memo(PageNavigation);