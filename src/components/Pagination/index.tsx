import React from 'react';import PageNumberSelector from "./PageNumberSelector";import PageNavigation from "./PageNavigation";interface IPagination {    pageLimit : number;    pageNumber: number;    paginationButtonNumber: number    dataLength: number;    handlerChangePageLimit : (e: React.ChangeEvent<HTMLSelectElement>)=>void;    handlerChangePageNumber : (direction? : "plus"| "minus") => (e:React.MouseEvent<HTMLButtonElement, MouseEvent>) => void;}function Pagination({pageLimit, pageNumber, paginationButtonNumber , dataLength , handlerChangePageNumber, handlerChangePageLimit}:IPagination) {    return (        <tfoot className="pagination">        <tr>            <th scope="row" colSpan={7}>            <PageNumberSelector pageLimit={pageLimit} handlerChangePageLimit={handlerChangePageLimit} />            <PageNavigation paginationButtonNumber={paginationButtonNumber} pageNumber={pageNumber} end={Math.ceil(dataLength / pageLimit)} handlerChangePageNumber={handlerChangePageNumber} />            </th>        </tr>        </tfoot>    );}export default Pagination;