import React from 'react';interface IPageButton {    value: number | string;    isSelected : boolean    isDirection?:  "plus"| "minus";    isDisabled?: boolean ;    handlerChangePageNumber: (direction? : "plus"| "minus") => (e:React.MouseEvent<HTMLButtonElement, MouseEvent>) => void;}function PageButton({value, isSelected,isDirection,handlerChangePageNumber, isDisabled}:IPageButton) {    return (        <button value={isDirection === undefined ? value : undefined} onClick={handlerChangePageNumber(isDirection)} type="button" className={ isSelected  ? "selected" : "" } disabled={isDisabled} >{value}</button>    );}PageButton.defaultProps = {    isDisabled: false,    isDirection: undefined};export default React.memo(PageButton);