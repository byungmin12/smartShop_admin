import React from 'react';interface ITableRow {    id: number;    title: string;    description : string;    price: number;    rating: number;    brand: string;}function TableRow({id,title,description,price,rating,brand}:ITableRow) {    return (        <tr>            <td key={id}>{id}</td>            <td key={title}>{title}</td>            <td key={brand}>{brand}</td>            <td key={description}>{description.length > 40 ?                `${description.slice(0,40)}...` : description}</td>            <td key={price}>{price}</td>            <td key={rating}>{rating}</td>        </tr>    );}export default TableRow;