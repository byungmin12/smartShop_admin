import React from 'react';interface ITableRow {    id: number;    title: string;    description : string;    price: number;    rating: number;    brand: string;}function TableRow({id,title,description,price,rating,brand}:ITableRow) {    return (        <tr>            <td >{id}</td>            <td >{title}</td>            <td >{brand}</td>            <td >{description.length > 40 ?                `${description.slice(0,40)}...` : description}</td>            <td >{price}</td>            <td >{rating}</td>        </tr>    );}export default TableRow;