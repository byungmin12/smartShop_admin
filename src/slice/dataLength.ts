import {createSlice, PayloadAction} from "@reduxjs/toolkit";import {RootState} from "../store/store"; export const dataLength = createSlice({    name: "dataLength",    initialState: 0,    reducers: {        setDataLength(state, action:PayloadAction<number>) {            return action.payload        }    }})export const {setDataLength}  = dataLength.actionsexport const getDataLength = (state: RootState) => state.dataLengthexport default dataLength.reducer