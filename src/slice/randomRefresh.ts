import {createSlice, PayloadAction} from "@reduxjs/toolkit";import {RootState} from "../store/store";export const randomRefresh = createSlice({    name: "randomRefresh",    initialState: Math.random,    reducers: {        setRandomRefresh(state, action:PayloadAction<number>) {            return action.payload        }    }})export const {setRandomRefresh}  = randomRefresh.actionsexport const getRefresh = (state: RootState) => state.randomRefreshexport default randomRefresh.reducer